document.addEventListener('DOMContentLoaded',()=>{const d=document.querySelector('.detail'),main=d?.querySelector('.detail-img');if(!main)return;const gallery=document.createElement('div');gallery.className='product-gallery';gallery.innerHTML=`<div class="thumbs">${[1,2,3,4].map((n,i)=>`<button class="thumb ${i?'':'active'}"><img src="${main.src.replace('multi_1','multi_'+n)}" alt="Product view ${n}"></button>`).join('')}</div>`;main.parentNode.insertBefore(gallery,main);gallery.appendChild(main);gallery.querySelectorAll('.thumb').forEach(b=>b.onclick=()=>{gallery.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));b.classList.add('active');main.src=b.querySelector('img').src})});
